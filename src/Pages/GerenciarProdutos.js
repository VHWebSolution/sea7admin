import React from 'react'
import GP from '../Components/GP'

const GerenciarProdutos = ({token}) => {
  return (
    <div className=''>
        <GP token={token}/>
    </div>
  )
}

export default GerenciarProdutos